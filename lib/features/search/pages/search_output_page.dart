import 'package:flutter/material.dart';

class SearchOutputPage extends StatelessWidget {
  const SearchOutputPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
